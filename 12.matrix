cat("Enter 3 integers for vector a (separated by spaces): ")
a <- as.numeric(unlist(strsplit(readline(), " ")))
cat("Enter 3 integers for vector b (separated by spaces): ")
b <- as.numeric(unlist(strsplit(readline(), " ")))
cat("Enter 3 integers for vector c (separated by spaces): ")
c <- as.numeric(unlist(strsplit(readline(), " ")))
matrix_3x3 <- cbind(a, b, c)
cat("\n3×3 matrix where each column represents a vector:\n")
print(matrix_3x3)
